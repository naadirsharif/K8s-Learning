apiVersion: v1
kind: Pod
metadata:
  name: secret-demo-Pod
spec:
  containers:
  - name: secrets-demo-container
    image: nginx
  env:
  - name: SECRET_USERNAME
    valueFrom: 
      secretKeyRef:
        name: my-secret
        key: username